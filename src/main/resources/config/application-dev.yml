# WEB PROPERTIES
server:
  port: 8080
  context-path: '/'

# SPRING
spring:
  # DATASOURCE
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: 'jdbc:mysql://localhost:3306/dyl-admin?useUnicode=true&characterEncoding=UTF-8'
    username: root
    password: 123456

  # JPA
  jpa:
    properties:
      hibernate:
        format_sql: true
        #show_sql: true